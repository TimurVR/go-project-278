var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { useRouterProvider } from "./RouterProviderContext.js";
/**
 * Outlet component for nested route rendering.
 * This is a router-agnostic wrapper that uses the configured router provider.
 *
 * @example
 * import { Outlet } from 'react-admin';
 *
 * const MyLayout = () => (
 *     <div>
 *         <header>My App</header>
 *         <main>
 *             <Outlet />
 *         </main>
 *     </div>
 * );
 */
export var Outlet = function (props) {
    var provider = useRouterProvider();
    return React.createElement(provider.Outlet, __assign({}, props));
};
//# sourceMappingURL=Outlet.js.map